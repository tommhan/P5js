import useState from "react";
import "./styles.css";
const lista0 = [1, 2, 3, 4, 5];
const lista1 = ["a", "s", "k"];

const Liita = ({ lista0, lista1 }) => {
  return (
    <div>
      <p> {lista0.concat(lista1).join(", ")}</p>
    </div>
  );
};

const App = () => {
  console.log("Hello from komponentti");

  return (
    <div>
      <p>Hello world</p>
      <Liita lista0={lista0} lista1={lista1} />
    </div>
  );
};

export default App;
